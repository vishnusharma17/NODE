// module.exports = 123;

// let obj = {
//   name: "vishnu",
//   age: 22,
//   address: "jaipur",
// };
// module.exports = obj;

// let sum = (a, b) => a + b;
// let sub = (a, b) => a - b;
// let mul = (a, b) => a * b;
// let obj2 = {
//   sum: sum,
//   sub: sub,
//   mul: mul,
// };
// module.exports = obj2;

//it is same like above code but in this we can directly export the function
// module.exports = {
//   sum: sum,
//   sub: sub,
//   mul: mul,
// };

//it is same like above code but in this we can directly export the function
// module.exports.sum = (a, b) => a + b;

//it is same like above code but in this we can directly export the function or sumtime he wants only object and in this object we can define the function
// exports.sum = (a, b) => a + b;

//import or export
export const name = "vishnu";
export const age = 22;
